package com.tflow.model.data.query;

public enum TableJoinType {
    NONE,

    INNER_JOIN,

    LEFT_JOIN,
    LEFT_OUTER_JOIN,

    RIGHT_JOIN,
    RIGHT_OUTER_JOIN,

    FULL_JOIN,
    FULL_OUTER_JOIN,

    OUTER_JOIN,
    ;
}
